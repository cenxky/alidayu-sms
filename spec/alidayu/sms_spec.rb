require "spec_helper"

RSpec.describe Alidayu::Sms do
  it "has a version number" do
    expect(Alidayu::Sms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
